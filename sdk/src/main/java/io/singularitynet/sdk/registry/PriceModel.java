package io.singularitynet.sdk.registry;

/**
 * Price model enum.
 */
public enum PriceModel {

    /**
     * Each call has fixed price which is contained in Pricing.
     * @see io.singularitynet.sdk.registry.Pricing
     */
    FIXED_PRICE

}
